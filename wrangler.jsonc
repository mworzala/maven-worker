{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "maven-worker",
  // Cloudflare account id. `npm run setup` fills this in.
  "account_id": "REPLACE_WITH_ACCOUNT_ID",
  "main": "src/index.ts",
  "compatibility_date": "2025-06-01",
  "compatibility_flags": ["nodejs_compat"],
  "observability": { "enabled": true },
  "r2_buckets": [
    {
      "binding": "BUCKET",
      "bucket_name": "maven-artifacts",
      "preview_bucket_name": "maven-artifacts-preview",
    },
  ],
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "maven",
      "database_id": "REPLACE_WITH_D1_DATABASE_ID",
      "migrations_dir": "migrations",
    },
  ],
  "triggers": { "crons": ["0 3 * * *"] },
  // Deployment paths are pure configuration. `host: "*"` matches any hostname.
  // Match precedence: exact host before "*", then longest path prefix.
  // Example custom domain: set REPOSITORIES to
  //   [{ host: "repo.example.com", prefix: "/releases", repo: "release", r2Prefix: "releases" },
  //    { host: "repo.example.com", prefix: "/snapshots", repo: "snapshot", r2Prefix: "snapshots" }]
  // and add a matching `route` for that hostname.
  "vars": {
    "REPOSITORIES": [
      { "host": "*", "prefix": "/releases", "repo": "release", "r2Prefix": "releases" },
      { "host": "*", "prefix": "/snapshots", "repo": "snapshot", "r2Prefix": "snapshots" },
    ],
  },
}
